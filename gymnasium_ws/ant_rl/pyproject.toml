[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "antpilot"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
antpilot = "main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["antpilot*"]